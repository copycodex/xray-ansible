- name: Export Xray clients via role
  hosts: app
  gather_facts: no
  become: yes
  vars:
    xray_export_dir: "exports/xray"  # local path for artifacts
  tasks:
    - name: Run export task from xray role
      include_role:
        name: xray
        tasks_from: export-clients.yml
